pipeline {
	def mvnHome
	stage('Build') {
		steps {
			echo 'prueba'
			//git url: 'https://github.com/manuexcd/spring-cloud-discovery'
			//sh "'${mvnHome}/bin/mvn' -Dmaven.test.failure.ignore clean package docker:build"
		}
	}
	//stage('Results') {
		//junit '**/target/surefire-reports/TEST-*.xml'
		//archive 'target/*.jar'
	//}
}